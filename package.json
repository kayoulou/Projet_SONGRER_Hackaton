{
  "name": "songrer-platform",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "songrer-api",
    "songrer-dashboard",
    "songrer-mobile"
  ],
  "scripts": {
    "api:dev": "npm run dev --workspace songrer-api",
    "api:migrate": "npm run prisma:migrate --workspace songrer-api",
    "api:seed": "npm run prisma:seed --workspace songrer-api",
    "dashboard:dev": "npm run dev --workspace songrer-dashboard",
    "mobile:start": "npm run start --workspace songrer-mobile"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  }
}
